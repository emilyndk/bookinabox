---

const { label, input } = Astro.props;
---
<div class="flex flex-col mt-xs text-forrest-500">
    <label class="text-h6-mobil" for="inputfelt">{label}</label>
<input class="bg-opacity-50 bg-cherry-500" id="inputfelt" type="text" placeholder={input}>
</div>